# Model-View-Controller-Movies

To see the http request, check Views/Home/Index.cshtml. It's just a basic html request, not necessarily related to the movies app.

It mocks some movies in Models/SeedData.cs.
